package context_only_all

import (
	"context"
	"log/slog"
)

func _(ctx context.Context) {
	slog.InfoContext("msg") // want `InfoContext should be used instead`
	slog.InfoContext(ctx, "msg")
}

func _() {
	slog.InfoContext("msg") // want `InfoContext should be used instead`
	slog.InfoContext(context.Background(), "msg")
}
